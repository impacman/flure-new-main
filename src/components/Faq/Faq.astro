---
import "./styles.scss";
import faq from "../../data/faq.json";

const { title, items } = faq;

import Title from "../Title";
import Items from "./Items";
---

<section class="faq">
  <div class="faq__wrap">
    <Title title={title} />
    <Items client:only="react" items={items} />
  </div>
</section>
